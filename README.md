# vault-ldap-namespace-policy-mapping
In multi-tenant Vault setups, managing auth and policies across namespaces can be complex. Vault Enterprise with identity groups, namespaces, and LDAP enables secure, centralized access. This guide covers configuring LDAP auth, mapping groups, and applying namespace-specific policies.
